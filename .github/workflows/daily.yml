name: NBA Daily Predictions - Full Pipeline

on:
  schedule:
    - cron: '0 6 * * *'  # Runs daily at 6:00 AM UTC (11:30 AM IST)
  workflow_dispatch:  # Allow manual trigger

jobs:
  predict:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      # ===== STEP 1: FETCH DATA =====
      - name: Create pre_match folder
        run: mkdir -p pre_match
      
      - name: Verify checked out files
        run: |
          echo "📂 Current directory:"
          pwd
          echo ""
          echo "📋 All files in repository:"
          ls -la
          echo ""
          echo "🐍 Python files:"
          ls -la *.py || echo "No Python files found in root"
      
      - name: Step 1 - Fetch today's matches and features
        run: |
          echo "🔍 Fetching today's NBA matches..."
          python Pre_Match.py
        continue-on-error: false
      
      - name: Step 2 - Extract odds and features
        run: |
          echo "📊 Extracting DraftKings odds and features..."
          python Odds_Pre_Match.py
        continue-on-error: false
      
      - name: Step 3 - Generate Future.csv (Combine data)
        run: |
          echo "🔗 Combining features into Future.csv..."
          if [ ! -f "Combine.py" ]; then
            echo "❌ ERROR: Combine.py not found!"
            ls -la
            exit 1
          fi
          python Combine.py
        continue-on-error: false
      
      - name: Verify Future.csv was created
        run: |
          echo "📋 Checking generated CSV files:"
          ls -lh *.csv 2>/dev/null || echo "No CSV files found in root"
          echo ""
          if [ -f "Future.csv" ]; then
            echo "✅ Future.csv generated successfully"
            echo "File size: $(ls -lh Future.csv | awk '{print $5}')"
            echo "Lines: $(wc -l Future.csv)"
          else
            echo "❌ ERROR: Future.csv not found!"
            exit 1
          fi
      
      # ===== STEP 2: GENERATE PREDICTIONS =====
      - name: Step 4 - Generate model predictions
        run: |
          echo "🚀 Running ML predictions..."
          python predict.py
        continue-on-error: false
      
      - name: Verify prediction output
        run: |
          echo "📊 Checking prediction outputs:"
          if [ -d "predictions_output" ]; then
            ls -lh predictions_output/
            echo ""
            echo "✅ Prediction files generated"
          else
            echo "⚠️ predictions_output directory not found"
          fi
          
          if [ -f "PREDICTIONS_LATEST.csv" ]; then
            echo "✅ PREDICTIONS_LATEST.csv exists"
          fi
      
      - name: Copy latest predictions to repo root
        run: |
          echo "📋 Copying prediction files to root..."
          cp predictions_output/PREDICTIONS_LATEST.csv ./ || echo "⚠️ PREDICTIONS_LATEST.csv not found"
          cp predictions_output/HIGH_CONFIDENCE_PICKS.csv ./ || echo "⚠️ HIGH_CONFIDENCE_PICKS.csv not found"
          cp predictions_output/SUMMARY_STATS.csv ./ || echo "⚠️ SUMMARY_STATS.csv not found"
          
          echo "📁 Files ready for commit:"
          ls -lh *.csv 2>/dev/null || echo "No CSV files in root"
      
      # ===== STEP 3: COMMIT & PUSH =====
      - name: Commit and push results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # Pull before pushing to avoid conflicts
          echo "🔄 Syncing with remote..."
          git pull --rebase origin main 2>/dev/null || git pull --rebase origin master 2>/dev/null || echo "Already up to date"
          
          # Add generated CSV files
          echo "📁 Adding prediction files..."
          git add -f nba_prematch_features.csv upcoming_nba_draftkings_odds.csv Future.csv PREDICTIONS_LATEST.csv HIGH_CONFIDENCE_PICKS.csv SUMMARY_STATS.csv 2>/dev/null || echo "Adding available files"
          
          # Check if there are changes
          if git diff --staged --quiet; then
            echo "ℹ️ No changes to commit"
            exit 0
          fi
          
          # Commit changes
          echo "✅ Changes detected, committing..."
          git commit -m "🤖 Auto: NBA predictions $(date +'%Y-%m-%d %H:%M UTC')"
          
          # Push with retry logic
          max_retries=5
          retry_count=0
          while [ $retry_count -lt $max_retries ]; do
            retry_count=$((retry_count+1))
            echo "📤 Push attempt $retry_count/$max_retries..."
            
            if git push origin main 2>&1; then
              echo "✅ Successfully pushed to main!"
              exit 0
            elif git push origin master 2>&1; then
              echo "✅ Successfully pushed to master!"
              exit 0
            fi
            
            if [ $retry_count -lt $max_retries ]; then
              echo "⏳ Retrying in 10 seconds..."
              sleep 10
              git pull --rebase origin main 2>&1 || git pull --rebase origin master 2>&1 || true
            fi
          done
          
          echo "❌ Failed to push after $max_retries attempts"
          exit 1
        continue-on-error: true
      
      # Upload as backup
      - name: Upload predictions as backup artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nba-predictions-${{ github.run_number }}
          path: |
            nba_prematch_features.csv
            upcoming_nba_draftkings_odds.csv
            Future.csv
            PREDICTIONS_LATEST.csv
            HIGH_CONFIDENCE_PICKS.csv
            SUMMARY_STATS.csv
          retention-days: 7
